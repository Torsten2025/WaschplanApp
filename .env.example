# ============================================================================
# Environment-Variablen fÃ¼r Waschmaschinen-App
# ============================================================================
# 
# Anleitung:
# 1. Kopieren Sie diese Datei zu .env
# 2. Passen Sie die Werte fÃ¼r Ihre Umgebung an
# 3. In Produktion: Setzen Sie sichere Werte fÃ¼r alle Variablen
#
# WICHTIG: Die .env Datei sollte NIEMALS ins Git-Repository committed werden!
# Die .env Datei ist bereits in .gitignore eingetragen.
# ============================================================================

# ----------------------------------------------------------------------------
# Server-Konfiguration
# ----------------------------------------------------------------------------

# Port, auf dem der Server lÃ¤uft
# Standard: 3000
PORT=3000

# Node.js Umgebung
# MÃ¶gliche Werte: development, production
# Standard: development
NODE_ENV=development

# ----------------------------------------------------------------------------
# CORS-Konfiguration (WICHTIG fÃ¼r Produktion!)
# ----------------------------------------------------------------------------

# Erlaubte Origins fÃ¼r CORS-Requests
# 
# Entwicklung:
#   - Einzelner Origin: ALLOWED_ORIGIN=http://localhost:3000
#   - Mehrere Origins: ALLOWED_ORIGIN=http://localhost:3000,http://localhost:3001
#   - Alle Origins (nur fÃ¼r Entwicklung!): Lassen Sie diese Variable leer
#
# Produktion:
#   - Setzen Sie ALLOWED_ORIGIN auf Ihre tatsÃ¤chliche Domain
#   - Beispiel: ALLOWED_ORIGIN=https://waschmaschine.example.com
#   - Mehrere Domains: ALLOWED_ORIGIN=https://app.example.com,https://admin.example.com
#
# WICHTIG: In Produktion sollte diese Variable IMMER gesetzt werden!
# Standard (wenn nicht gesetzt):
#   - Development: * (alle Origins erlaubt)
#   - Production: http://localhost:3000 (nur localhost)
ALLOWED_ORIGIN=

# ----------------------------------------------------------------------------
# Session-Konfiguration
# ----------------------------------------------------------------------------

# Secret-Key fÃ¼r Session-VerschlÃ¼sselung
# 
# WICHTIG: In Produktion MUSS ein sicherer, zufÃ¤lliger Wert gesetzt werden!
# Generieren Sie einen sicheren Key mit:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Standard: waschmaschine-secret-key-change-in-production
# 
# Beispiel fÃ¼r Produktion:
# SESSION_SECRET=your-super-secret-random-key-here-minimum-32-characters
SESSION_SECRET=waschmaschine-secret-key-change-in-production

# ----------------------------------------------------------------------------
# Beispiel-Konfigurationen
# ----------------------------------------------------------------------------

# Entwicklung (localhost):
# PORT=3000
# NODE_ENV=development
# ALLOWED_ORIGIN=http://localhost:3000
# SESSION_SECRET=dev-secret-key

# Produktion:
# PORT=3000
# NODE_ENV=production
# ALLOWED_ORIGIN=https://waschmaschine.example.com
# SESSION_SECRET=<generierter-sicherer-key>
